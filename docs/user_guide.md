# ForexAI Pro — User Guide

Welcome to the ForexAI platform natively assisting Traders across highly leveraged markets dynamically!

## 1. Connecting to the Matrix
After creating an account utilizing standard Auth flow natively mapping tokens to your frontend bounds, you govern your private trading universe.

## 2. Using Artificial Intelligence Arrays
The system is built sequentially to prevent emotion-driven investing:
1. **Submit structural Analysis**: Ping the Ensembles asking for structural layouts across explicit limits (e.g. `GBP/USD` on `15m` boundaries).
2. **Review the Data**: The Ensemble natively generates an overarching `TradeGrade` constraint bounds (e.g., `A+`, `C`, `WAIT`). Do not execute `C` grades!
3. **Read the Logic**: SHAP Extractors will explicitly list *why* the matrix suggests what it does implicitly generating simple natural language structures (e.g., "RSI bounds heavily oversold").

## 3. RAG Knowledge
Upload `.pdf` charts mapped dynamically to constraints across personal limits. The AI parses the pages bounds structurally parsing paragraphs answering trading thesis contexts directly!

## 4. Journaling
Every AI prediction output bounds saves natively! You simply report whether you won or lost mapping Actual PNL to structural boundaries!
