# ForexAI Pro — Administrator Guide

The System Administrator (Root Access) has total control over the foundational boundaries of the application and the ML logic. To gain admin tools, manually patch your User Account via PostgreSQL mapping `role='admin'` securely.

## 1. Training & Promoting Models
Access structural bounds mapped inherently at `/api/v1/admin/models`. You govern which AI logic governs the Ensemble mapping limits natively:
- **Retraining**: Request targeted historical generation spanning EUR/USD limits natively!
- **Promotions**: Evaluates XGBoost output bounds structurally testing validations against native algorithms. If the Sharpe ratios exceed previous builds, you promote them live universally!

## 2. Broadcast Notifications
You map native System Alerts alerting every trader asynchronously via `/api/v1/admin/notifications/broadcast` enforcing system down times!

## 3. Metrics Diagnostics
Ping endpoints spanning `/api/v1/admin/metrics` to read active RAM thresholds preventing memory leaks explicitly!
